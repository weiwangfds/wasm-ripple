{
  "name": "wasm-mq-demo",
  "version": "0.1.0",
  "scripts": {
    "build": "wasm-pack build --target web && node scripts/post-build.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {}
}
